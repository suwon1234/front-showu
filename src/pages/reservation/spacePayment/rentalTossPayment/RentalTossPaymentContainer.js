import React from 'react';
import { Outlet } from 'react-router-dom';

const RentalTossPaymentContainer = () => {
  return (
    <div>
      <Outlet/>
    </div>
  );
};

export default RentalTossPaymentContainer;