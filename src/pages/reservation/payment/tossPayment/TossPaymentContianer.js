import React from 'react';
import { Outlet } from 'react-router-dom';

const TossPaymentContianer = () => {
  return (
    <div>
      <Outlet/>
    </div>
  );
};

export default TossPaymentContianer;